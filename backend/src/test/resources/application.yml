spring:
  datasource:
    url: jdbc:mysql://localhost:3306/concert_booking_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root123
    hikari:
      maximum-pool-size: 50
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update # Để test tự tạo bảng
    show-sql: false
  hikari:
    maximum-pool-size: 50
    connection-timeout: 30000
